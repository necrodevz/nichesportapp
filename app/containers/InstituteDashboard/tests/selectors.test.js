// import { fromJS } from 'immutable';
// import { makeSelectInstituteDashboardDomain } from '../selectors';

// const selector = makeSelectInstituteDashboardDomain();

describe('makeSelectInstituteDashboardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
